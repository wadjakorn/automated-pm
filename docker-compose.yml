services:
  web:
    build: .
    container_name: automated-pm
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PM_DB_PATH=/app/data/pm.db
    restart: unless-stopped
